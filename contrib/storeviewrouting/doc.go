// Package storeviewrouting provides PromQL-aware metric store view routing.
//
// It is distributed as a separate module so the root SLS SDK does not need to
// depend on Prometheus or raise its minimum supported Go version.
package storeviewrouting
